import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3e3f3a',
    },
    secondary: {
      main: '#de62828',
    },
    common: {
      drawer: '#A22032',
    },
    type: 'dark',
  },
});

export default theme;
