import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3e3f3a',
    },
    secondary: {
      main: '#fff',
    },
    common: {
      drawer: '#a99561',
    },
    type: 'dark',
  },
});

export default theme;
